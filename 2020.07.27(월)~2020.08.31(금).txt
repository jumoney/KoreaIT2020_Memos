2020.07.27(월)

오전수업(JSP)
- 

2020.07.28(화)

오후수업(JavaScript)
- 함수

자바스크립트에서는

메소드 - 객체의 멤버로 들어가 있는 것
함수 - function 키워드 만들어 사용하는 것

2020.07.29(화)

오전수업(JSP)
- GET방식과 POST방식이 같은 값을 전달한다면 GET으로 온 값이 우선한다.
- 패키지명은 무조건 소문자(중간에도 대문자를 쓰면 안된다.) 클래스는 무조건 첫문자를 대문자로.
- 레퍼런스변수 앞의 final을 붙이면 주소값을 못바꾼다.(주소만! 주소값안의 값은 변경이 가능하다.)

오후수업(JavaScript)
- 기본 함수는 호이스팅을 지원하지만 익명 함수는 호이스팅을 지원하지 않는다.

2020.07.30(목)
오후수업(Javascript)
- 

2020.07.31(금)
오전수업(JSP)
- 태그 name속성은 서버에서 키값이다.(나머지 class나 id는 키값으로 사용할 수 없다.)
- id는 유일성을 확보하기 위한 용도이다.(unique다. 써도 되지만 올바른 사용법이 아니다.)
- class는 중복이 가능하다 그룹으로 묶을때 사용한다.(여러 태그에 같은 CSS를 적용하고 싶다던지 하는 경우)
- https://flatuicolors.com/ (color보는 사이트)

오후수업(JavaScript)
- 
