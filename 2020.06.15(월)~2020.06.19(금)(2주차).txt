2020.06.15(월)
- 중복된 소스는 죄.(가능한 중복된 소스를 줄여야한다.)
- 리터럴값은 되도록 바로 쓰지 않도록 한다. ex) if(height > 150)

2020.06.16(화)
- 자바에서는 배열 선언 할때 "int[]"로 표기해 주는게 좋다 int arr[]로 사용할 경우
오해의 소지가 있다.(int가 마치 일반 변수인거 처럼 보일 수 있다.)

2020.06.17(수)
- break문: 가장 가까이에 있는 "반복문" 하나를 종료시킨다.

2020.06.18(목)
- workspace변경시 설정이 초기화되기 때문에 인코딩 설정부터 먼저해주자.
- 메소드 안에 메소드란 건 없다.
- 메소드는 void라도 return키워드는 무조건 들어간다.(이클립스에서는 안적어도 void면 자동으로 넣어주는것이다.)
- 메소드명은 동사로.
- 필기문제:
응용SW기초 기술 활용책 에서 나옴
(주관식)
cmd(윈도우즈 명령 프롬프트)명령어들 중:
폴더를 생성하는 명령어
폴더를 삭제하는 명령어
ip 주소를 확인하는 명령어(ip config)
현재시간을 확인하는 명령어
(명령어는 구글링)

운영체제 핵심기능 2가지:
메모리관리, 프로세스 관리

설명을 듣고 알맞은 운영체제 이름적기:
ex) 리눅스, 유닉스, 맥os, 윈도우즈

데이터베이스 설명 듣고 알아맞히기:
 r-db(관계형 데이터베이스), 계층형, 망형(3가지)

데이터베이스 기본연산 CRUD(Create, Read, Update, Delete)
insert
select
update
delete

네트워크기초활용하기에 나옴:
OSI 7계층 거의다 적어야 됨(순서대로, 순서만 외울것 설명까지 안나옴).

프로토콜에 대한 설명:
UDP, IPX
TCP/IP

2020.06.19(금)
- public static void sum(int ...arr) {("..."키워드는 무조건 끝에 넣어줄것 범위측정이 불가능하다.)
- 메소드안에 선언한 변수는 지역변수 바깥에 하면 전역변수.
-클래스 네임옆"( )"는 기본 생성자라고 부른다.

- super() : 내 직속부모를 뜻함.
- this : 나를(객체화 되었을때) 뜻함.
- 클래스의 생성자는 super();를 자동으로 넣어준다.
ex)
public Car() {
	super();
	System.out.println("Car 기본생성자!");
}
- this( )는 생성자 호출이다.

2020.06.22(월)

- default: 패키지 안에서만 접근 가능(안적으면 자동으로 default)
- public: 전체 공개(어디서나 접근가능
- private: 같은 클래스 안에서만
- protective: 같은패키지내의 상속관계에서만
- 모든객체가 같은값을 가졌으면 하는 경우 static으로 맴버변수를 선언해준다.
ex) static String contry;
- 맴버필드를 사용하지 않는 메소드는 무조건 static을 붙여주자.
- 전역변수: class바로 아래에 선언된 변수.(맴버필드)

html(오후수업)
- 육치성 학과장
이메일: captain6@naver.com

B강의실 공유폴더 접근방법
\\192.168.2.254

웹페이지
html5 - 뼈대
css3 - 장식(색깔, 크기, 모양)

- 주석 단축키(비쥬얼코드) 스크랩후 crt + /

2020.06.23(화)
- 클래스파일명과 똑같은 클래스만 앞에 public 등을 붙일수 있다.

2020.06.24(수)
- 이클립스 한 파일내에 화면 2개 분할 단축키(crt + shift + -(백스페이스 옆에 있는)

오후수업(HTML)
-파일 기준 상위 폴더 경로를 표시할때는 "../"를 사용한다.(상위 폴더에 이미지가 있는 경우)
ex)  <img src="../cover.jpg" alt="빠른연산">

2020.06.26(금)
- 오버로딩에서 리턴타입은 아무 관계가 없다(무조건 메개변수가 달라야만 한다.)
- 오버라이딩에서는 무조건 부모 메소드와 형식이 똑같아야 한다.
- 다형성의 이해.
	1. (상속관계)부모타입은 자식 객체 주소값을 저장할  수 있다.(가리킬 수 있다.)
	2. 자식타입은 부모 객체 주소값을 저장할 수 없다.(가리킬 수 없다.)
	3. 타입은 메소드 호출 할 수 있나 없나 결정!!! (자기가 알고 있는 메소드만 호출할 수 있다.)
	   호출 했을때는 객체 기준으로 호출이 된다.

오후수업(HTML)





