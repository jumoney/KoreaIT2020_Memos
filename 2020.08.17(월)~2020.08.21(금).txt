2020.08.17(월)
오전수업(Javascript)
- web.xml에 한글필터를 넣을때는 필터네임에 스프링이 들어가있는지 확인을 해라(jsp는 스프링이 없어야한다.)
- 한글필터설정을 해주는 이유는 POST방식일 때 한글이 깨지지 않게 하기 위해서 이다.(GET방식이 깨지지 않는 이유는
톰캣에서 UTF-8을 설정해 놓았기 때문이다.)
- get방식은 주로 화면출력을 할때 사용한다.(delete업무 빼고는)
- post는 업무 처리를 할 때 주로 사용한다.(주로 100%는 아니다.)

오후수업(jsp)
- DB쿼리(시퀀스 생성):
CREATE SEQUENCE seq_user
nocache;

2020.08.18(화)
오전수업(JSP)
- 인터페이스는 객체화가 되지 않는다.
- 인터페이스 안에서의 메소드선언시 public abstract 는 자동으로 넣어준다.

2020.08.19(수)
- 레퍼런스 변수에 final 을 붙이는 것은 주소값을 변경할 수 없다는 의미이다.(객체값 안의 내용은 변경이 가능하다.)
- C:\spring\apache-tomcat-9.0.37\lib 에 jstl-1.2.jar 와 taglibs-standard-impl-1.2.5.jar 파일을 추가해 준다.(jstl추가하는법)
- 주소앞에 '/'를 붙이지 않으면 원래 주소 뒤에 붙는다. 처음부터 주소를 시작하고 싶으면 앞에'/'를 붙여준다.
	ex) <a href="/board/reg"></a>

